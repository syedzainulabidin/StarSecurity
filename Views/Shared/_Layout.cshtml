<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - StarSecurity</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/StarSecurity.styles.css" asp-append-version="true" />
    <script src="https://unpkg.com/lucide@latest"></script>
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Anton&family=Space+Grotesk:wght@300;400;500;600;700&display=swap"
          rel="stylesheet">
    <link href="~/wwwroot/css/site.css" rel="stylesheet" />


    <style>
        body {
            font-family: 'Space Grotesk', sans-serif;
        }

        .font-anton {
            font-family: 'Anton', sans-serif;
        }

        .text-stroke {
            -webkit-text-stroke: 1px rgba(255, 255, 255, 0.2);
        }


        @@keyframes infinite-scroll {
            from

        {
            transform: translateX(0);
        }

        to {
            transform: translateX(-50%);
        }

        }

        .animate-infinite-scroll {
            animation: infinite-scroll 10s linear infinite;
        }

        .rotate-text {
            animation: rotate 10s linear infinite;
        }

        @@keyframes rotate {
            from

        {
            transform: rotate(0deg);
        }

        to {
            transform: rotate(360deg);
        }

        }

        body {
            font-family: 'Space Grotesk', sans-serif;
            background-color: #020617;
            color: white;
        }

        .font-anton {
            font-family: 'Anton', sans-serif;
        }

        [x-cloak] {
            display: none !important;
        }

        /* Custom mask/gradient for the central text */
        .text-gradient {
            background: linear-gradient(to bottom, #ffffff 50%, rgba(255, 255, 255, 0.2) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .team-image-container {
            position: relative;
            overflow: hidden;
            border-radius: 2.5rem;
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
        }

            .team-image-container:hover {
                transform: scale(0.98);
            }

            .team-image-container img {
                width: 100%;
                height: 100%;
                object-fit: cover;
                transition: transform 0.7s ease;
            }

            .team-image-container:hover img {
                transform: scale(1.1);
            }

        .team-image-overlay {
            position: absolute;
            inset: 0;
            background: linear-gradient(to top, rgba(2, 6, 23, 0.8) 0%, transparent 60%);
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .team-image-container:hover .team-image-overlay {
            opacity: 1;
        }

        .text-outline {
            -webkit-text-stroke: 1px rgba(255, 255, 255, 0.4);
            color: transparent;
        }

        .footer-link:hover {
            color: #2563eb;
            transition: color 0.3s ease;
        }


        body {
            font-family: 'Space Grotesk', sans-serif;
            background-color: #020617;
            color: white;
        }

        .font-anton {
            font-family: 'Anton', sans-serif;
        }

        .text-stroke {
            -webkit-text-stroke: 1px rgba(255, 255, 255, 0.2);
        }

        .text-gradient {
            background: linear-gradient(to bottom, #ffffff 50%, rgba(255, 255, 255, 0.2) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .team-image-container {
            position: relative;
            overflow: hidden;
            border-radius: 2.5rem;
            transition: all 0.5s ease;
        }

            .team-image-container img {
                width: 100%;
                height: 100%;
                object-fit: cover;
            }

        .team-image-overlay {
            position: absolute;
            inset: 0;
            background: linear-gradient(to top, rgba(2, 6, 23, 0.9) 0%, transparent 70%);
            display: flex;
            flex-direction: column;
            justify-content: flex-end;
            padding: 2rem;
        }


        body {
            font-family: 'Space Grotesk', sans-serif;
            background-color: #020617;
            color: white;
        }

        .font-anton {
            font-family: 'Anton', sans-serif;
        }

        .text-stroke {
            -webkit-text-stroke: 1px rgba(255, 255, 255, 0.2);
        }

        .job-row:hover {
            background-color: rgba(37, 99, 235, 0.05);
            border-color: rgba(37, 99, 235, 0.3);
        }

        .apply-btn:hover {
            background-color: #1d4ed8;
            transform: scale(1.05);
        }

        .card:hover {
            border-color: rgba(37, 99, 235, 0.4);
            transform: translateY(-5px);
        }


        body {
            font-family: 'Space Grotesk', sans-serif;
            background-color: #020617;
            color: white;
        }

        .font-anton {
            font-family: 'Anton', sans-serif;
        }

        .text-stroke {
            -webkit-text-stroke: 1px rgba(255, 255, 255, 0.2);
        }

        @@keyframes infinite-scroll {
            from

        {
            transform: translateX(0);
        }

        to {
            transform: translateX(-50%);
        }

        }

        .animate-infinite-scroll {
            animation: infinite-scroll 30s linear infinite;
        }

        /* Scroll Animations */
        .reveal {
            opacity: 0;
            transition: all 1s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .reveal-left {
            transform: translateX(-50px);
        }

        .reveal-right {
            transform: translateX(50px);
        }

        .reveal-up {
            transform: translateY(50px);
        }

        .reveal.active {
            opacity: 1;
            transform: translate(0, 0);
        }

        .input-glow:focus {
            box-shadow: 0 0 15px rgba(37, 99, 235, 0.3);
            border-color: rgba(37, 99, 235, 0.5);
        }


        body {
            font-family: 'Space Grotesk', sans-serif;
            background-color: #020617;
            color: white;
        }

        .font-anton {
            font-family: 'Anton', sans-serif;
        }

        .text-stroke {
            -webkit-text-stroke: 1px rgba(255, 255, 255, 0.2);
        }

        .reveal {
            opacity: 0;
            transition: all 1s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .reveal-up {
            transform: translateY(30px);
        }

        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }

        .glass-card {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        body {
            font-family: 'Space Grotesk', sans-serif;
            background-color: #020617;
            color: white;
        }

        .font-anton {
            font-family: 'Anton', sans-serif;
        }

        .text-stroke {
            -webkit-text-stroke: 1px rgba(255, 255, 255, 0.2);
        }

        .reveal {
            opacity: 0;
            transition: all 1s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .reveal-up {
            transform: translateY(50px);
        }

        .reveal.active {
            opacity: 1;
            transform: translate(0, 0);
        }

        input:focus {
            outline: none;
            border-color: #3b82f6;
            box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.2);
        }

        body {
            font-family: 'Space Grotesk', sans-serif;
            background-color: #020617;
            color: white;
        }

        .font-anton {
            font-family: 'Anton', sans-serif;
        }

        .text-stroke {
            -webkit-text-stroke: 1px rgba(255, 255, 255, 0.2);
        }

        @@keyframes infinite-scroll {
            from

        {
            transform: translateX(0);
        }

        to {
            transform: translateX(-50%);
        }

        }

        .animate-infinite-scroll {
            animation: infinite-scroll 30s linear infinite;
        }

        .testimonial-card {
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        }

            .testimonial-card:hover {
                transform: translateY(-10px);
                background-color: rgba(30, 58, 138, 0.3);
                border-color: rgba(59, 130, 246, 0.5);
                box-shadow: 0 20px 40px -20px rgba(59, 130, 246, 0.3);
            }
    </style>

</head>
<body class="bg-[#020617] text-white overflow-x-hidden">
    @* <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">StarSecurity</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        <!-- Inside the <nav> or <ul class="navbar-nav"> -->
                        <li class="nav-item">
                            <a class="nav-link" href="/">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/about">About Us</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/services">Our Business</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/network">Our Network</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/careers">Careers</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/testimonials">Testimonials</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/contact">Contact Us</a>
                        </li>
                        <!-- Add Login link if not logged in -->
                        @if (!User.Identity.IsAuthenticated)
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="/login">Login</a>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header> *@

    <nav class="relative z-50 flex items-center justify-between px-8 py-6 lg:px-16">
        <div class="flex items-center gap-2">
            <svg class="h-8 w-8 text-blue-600 fill-blue-600/20" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                 stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
            </svg>
            <span class="text-2xl font-bold tracking-tighter">GUARDITY</span>
        </div>

        <div class="hidden lg:flex items-center gap-8">
            <a href="/" class="text-sm font-medium text-blue-500">Home</a>
            <a href="/about" class="text-sm font-medium hover:text-blue-500 transition-colors">About</a>
            <a href="/network" class="text-sm font-medium hover:text-blue-500 transition-colors">Network</a>
            <a href="/services" class="text-sm font-medium hover:text-blue-500 transition-colors">Services</a>
            <a href="/inquire" class="text-sm font-medium hover:text-blue-500 transition-colors">Inquire</a>
            <a href="/careers" class="text-sm font-medium hover:text-blue-500 transition-colors">Career</a>
            <a href="/testimonials" class="text-sm font-medium hover:text-blue-500 transition-colors">Testimonials</a>
            <a href="/contact" class="text-sm font-medium hover:text-blue-500 transition-colors">Contact</a>
        </div>

        <div class="flex items-center gap-4">
            <a href="/login" class="flex items-center gap-2 rounded-md bg-blue-600 px-6 py-2.5 text-sm font-semibold text-white transition-all hover:bg-blue-700">
                Login
                <svg class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M7 7h10v10M7 17 17 7" />
                </svg>
            </a>
        </div>
    </nav>
    <div class="idk-container">
        <main role="main">
            @RenderBody()
        </main>
    </div>

    <footer class="relative pb-0 px-8 lg:px-16 max-w-7xl mx-auto">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12 py-16">
            <!-- Brand & Info -->
            <div class="space-y-6">
                <div class="flex items-center gap-2">
                    <div class="bg-blue-600 p-2 rounded-lg">
                        <i data-lucide="shield" class="w-6 h-6 text-white fill-white/20"></i>
                    </div>
                    <span class="text-3xl font-bold tracking-tighter uppercase font-anton">GUARDITY</span>
                </div>
                <p class="text-gray-400 leading-relaxed max-w-xs">
                    Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore
                    et dolore magna aliqua.
                </p>
                <!-- Social Icons -->
                <div class="flex items-center gap-3">
                    <a href="#"
                       class="w-10 h-10 rounded-full bg-blue-600 flex items-center justify-center hover:bg-blue-700 transition-colors">
                        <i data-lucide="facebook" class="w-4 h-4 fill-white"></i>
                    </a>
                    <a href="#"
                       class="w-10 h-10 rounded-full bg-blue-600 flex items-center justify-center hover:bg-blue-700 transition-colors">
                        <i data-lucide="twitter" class="w-4 h-4 fill-white"></i>
                    </a>
                    <a href="#"
                       class="w-10 h-10 rounded-full bg-blue-600 flex items-center justify-center hover:bg-blue-700 transition-colors">
                        <i data-lucide="youtube" class="w-4 h-4 fill-white"></i>
                    </a>
                    <a href="#"
                       class="w-10 h-10 rounded-full bg-blue-600 flex items-center justify-center hover:bg-blue-700 transition-colors">
                        <i data-lucide="instagram" class="w-4 h-4 text-white"></i>
                    </a>
                </div>
            </div>

            <!-- Our Pages -->
            <div class="space-y-6">
                <h4 class="text-blue-600 font-bold text-lg uppercase tracking-wider font-anton">Our Pages</h4>
                <ul class="space-y-4">
                    <li><a href="#" class="footer-link font-medium">Home Page</a></li>
                    <li><a href="#" class="footer-link font-medium">About Us</a></li>
                    <li><a href="#" class="footer-link font-medium">Our Services</a></li>
                    <li><a href="#" class="footer-link font-medium">Pricing Plan</a></li>
                </ul>
            </div>

            <!-- Quick Links -->
            <div class="space-y-6">
                <h4 class="text-blue-600 font-bold text-lg uppercase tracking-wider font-anton">Quick Links</h4>
                <ul class="space-y-4">
                    <li><a href="#" class="footer-link font-medium">Our Team</a></li>
                    <li><a href="#" class="footer-link font-medium">Career</a></li>
                    <li><a href="#" class="footer-link font-medium">Our Blog</a></li>
                    <li><a href="#" class="footer-link font-medium">Contact Us</a></li>
                </ul>
            </div>

            <!-- Get In Touch -->
            <div class="space-y-6">
                <h4 class="text-blue-600 font-bold text-lg uppercase tracking-wider font-anton">Get In Touch</h4>
                <ul class="space-y-4">
                    <li class="flex items-center gap-3">
                        <i data-lucide="phone" class="w-5 h-5 text-blue-600"></i>
                        <span class="font-medium text-gray-300">(+23) 456-867-908</span>
                    </li>
                    <li class="flex items-center gap-3">
                        <i data-lucide="mail" class="w-5 h-5 text-blue-600"></i>
                        <span class="font-medium text-gray-300">guardity@site.com</span>
                    </li>
                    <li class="flex items-center gap-3">
                        <i data-lucide="map-pin" class="w-5 h-5 text-blue-600"></i>
                        <span class="font-medium text-gray-300">East Coast City</span>
                    </li>
                </ul>
            </div>
        </div>
    </footer>
    <script>
        lucide.createIcons();
    </script>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
